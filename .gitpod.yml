image:
  file: .gitpod.Dockerfile

ports:
  - port: 3000
    onOpen: open-preview

tasks:
  - init: "npm install && echo '\x \\ CREATE DATABASE workshop;' | psql && psql -U gitpod workshop < dump.sql"
    command: "cd bookmonkey-rest-api && npm run start"
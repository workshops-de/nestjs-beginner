image:
  file: .gitpod.Dockerfile

ports:
  - port: 3000
    onOpen: open-preview

tasks:
  - before: cd database && cat dump.sql | mysql
    init: cd bookmonkey-rest-api && npm install
    command: cd bookmonkey-rest-api && npm run start
